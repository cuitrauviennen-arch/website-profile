{
  "kind": "collectionType",
  "collectionName": "projects",
  "info": {
    "singularName": "project",
    "pluralName": "projects",
    "displayName": "Project",
    "description": "Worked Projects Portfolio"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "description": "Hero Section - Tên chính của dự án. (VD: Global E-commerce Campaign)"
    },
    "slug": {
      "type": "uid",
      "targetField": "title",
      "description": "Đường dẫn URL của dự án."
    },
    "category": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text"
    },
    "link": {
      "type": "string"
    },
    "spanClass": {
      "type": "enumeration",
      "enum": [
        "span1",
        "span2",
        "span3"
      ],
      "default": "span1",
      "required": true,
      "description": "Kích thước thẻ hiển thị ngoài trang chủ."
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": [
        "images"
      ],
      "description": "Ảnh Thumbnail hiển thị ngoài trang chủ (Danh sách dự án). Khuyến nghị tỉ lệ 16:9 (1280x720px), dưới 500KB."
    },
    "order": {
      "type": "integer",
      "default": 0
    },
    "metric": {
      "type": "string"
    },
    "metricLabel": {
      "type": "string"
    },
    "eyebrow": {
      "type": "string",
      "description": "Hero Section - Tiêu đề phụ nằm trên cùng (VD: Performance Marketing · 2024)."
    },
    "summary": {
      "type": "text",
      "description": "Hero Section - Tóm tắt ngắn gọn dự án (1-3 câu)."
    },
    "overview": {
      "type": "component",
      "repeatable": true,
      "component": "project.overview-item",
      "description": "Section 2 (Project Overview): Thường thêm 3 thẻ để hiển thị đẹp nhất trên web."
    },
    "metrics": {
      "type": "component",
      "repeatable": true,
      "component": "project.metric",
      "description": "Section 3 (Key Metrics): Hiển thị dạng vòng tròn (rings)."
    },
    "phases": {
      "type": "component",
      "repeatable": true,
      "component": "project.phase",
      "description": "Section 4 (Strategy & Execution): Hiển thị đan xen (zíc-zắc) ảnh và chữ."
    },
    "assets": {
      "type": "component",
      "repeatable": true,
      "component": "project.creative-asset",
      "description": "Section 5 (Creative Assets): Khu vực lưới Bento (dọc & vuông)."
    },
    "nextProject": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::project.project"
    }
  }
}
